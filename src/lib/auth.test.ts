// Ce fichier a été remplacé par validation.test.ts
// (les fonctions pures sont maintenant dans validation.ts pour ne pas
// dépendre de Supabase au moment des tests).
//
// Vitest ignore ce fichier car il ne contient pas de bloc describe().

import { describe } from 'vitest';
describe.skip('placeholder', () => {});
